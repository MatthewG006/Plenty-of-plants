
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access for all files.
    // This is safe for public assets like the fallback plant images.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
