# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# By removing the runConfig, we will use the default timeouts, which are
# more appropriate for this application and resolve the startup probe conflict.
